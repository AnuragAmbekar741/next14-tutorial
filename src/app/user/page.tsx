import UserCard from "@/components/UserCard";
import React from "react";

const user = () => {
  return (
    <div className="w-full flex p-10">
      <UserCard />
    </div>
  );
};

export default user;
